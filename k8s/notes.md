minikube creates local kubernetes environment inside docker
helm is package manager for kubernetes

eval $(minikube docker-env)
    -> use this to get inside the minikube shell and run things there (but still be on local folder)

portainer is installed inside minikube so that it can monitor the kubernetes cluster running in minikube 



